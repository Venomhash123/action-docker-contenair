name: container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container:
      image: idkknow:wz
    steps:
         - name: Log the node version
           run: |
             node -v
             cat /etc/os-release  
